#pragma once
#include <string_view>

namespace Config {

    // Window params
    const int WINDOW_HEIGHT = 800;
    const int WINDOW_WIDTH = 600;

    // Viewport params
    const float BOX_HALF_EXTENT = 200.0f;

    // Simulation params
    const int N = 800;
}
